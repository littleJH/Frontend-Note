## 类型结构图

![image-20250917104857060](https://jiahe-picbed.oss-cn-shenzhen.aliyuncs.com/typora-image/image-20250917104857060.png)

## any 与 unknown

`any` 能够兼容所有类型，也能够被所有类型兼容

`unknown` 能够被赋值为 **任何类型**，但只能赋值给 `any` 或 `unknown`

`any` 放弃了所有 **类型检查**，而 `unknown` 没有

```typescript
let a: unknonw;
let b: any;

a.foo();	// error
b.foo();
```



## JS 数据类型

### string

### number

### boolean

### null

### undefined

### symbol

### bigint

### object

## void

在 TS 中，void 也是一种类型，用于描述内部 **没有返回值** 的函数，或者没有显式 `return` 一个值。

## never

`never` 类型不携带任何类型信息，是整个类型系统中 **最底层** 的类型

如果说 `any` `unknown` 是其他类型的 **父类型**，那么 `never` 就是其他所有类型的 **子类型**

因此，`never` 类型可以赋值给其他所有类型，反之则不行



显式声明一个变量为 `never` 是没有意义的，但是在实际开发中，有以下两种应用场景

#### 控制流分析

```typescript
type Method = "GET" | "POST";

function request(url: string, method: Method) {
  if (method === "GET") {
    console.log(method); // GET
    // todos...
  }
  else if (method === "POST") {
    console.log(method); // POST
    // todos...
  }
  else {
    console.log(method) // never
  }
}
```

上面的代码没有什么问题，但是如果某一天，`Method` 类型加入了新的联合类型，比如 `type Method = "GET" | "POST" | "PUT" | "DELETE";`，特别是在团队开发中，这个时候，request 函数是没有任何感知的。

```typescript
type Method = "GET" | "POST" | "PUT" | "DELETE";

function request(url: string, method: Method) {
  if (method === "GET") {
    console.log(method); // GET
    // todos...
  }
  else if (method === "POST") {
    console.log(method); // POST
    // todos...
  }
  else {
    const _neverCheck: never = method;
    throw new Error(`不知道的类型: ${_neverCheck}`);
  }
}
```

将代码修改为现在的这个样子，虽然现在有报错了，**`method` 根据类型流分析，还剩下 `"PUT" | "DELETE"` 类型，所以不能赋值给 `never` 类型**。但是将错误扼杀在摇篮中，才是在团队项目中想要的结果，而不是等运行了，才去一个个排查，特别是这种隐藏的 bug，在团队的成千上万行代码与模块中，去找到这个问题，是非常痛苦的问题。

> 这种方式也叫做 **穷举式检查**，积极的对不期望的情况进行错误处理，在编译时就捕获未处理的情况。而不是默默地忽略它们

比如，前面的代码，我们也可以进行修改：

```typescript
type Circle = { kind: "circle", radius: number }
type Rect = { kind: "rect", width: number, height: number }
type Shape = Circle | Rect;

function area(shape: Shape) {
  switch (shape.kind) {
    case "circle":
      return Math.PI * shape.radius ** 2;
    case "rect":
      return shape.width * shape.height;
    default:
      const _neverCheck: never = shape; 
      throw new Error("Invalid shape type");
  }
}
```

如果新加一个类型 `const _neverCheck: never = shape;` 这行代码就会报错，因为控制流分析并没有完全结束

```diff
type Circle = { kind: "circle", radius: number }
type Rect = { kind: "rect", width: number, height: number }
+type Triangle = { kind: "triangle", base: number, height: number }
type Shape = Circle | Rect | Triangle;

function area(shape: Shape) {
  switch (shape.kind) {
    case "circle":
      return Math.PI * shape.radius ** 2;
    case "rect":
      return shape.width * shape.height;
+    case "triangle":
+      return shape.base * shape.height / 2;
    default:
      const _neverCheck: never = shape; 
      throw new Error("Invalid shape type");
  }
}
```



#### 抛出错误

调用了返回值为 `never` 类型的函数，那么该函数下面的代码会被视为无效代码

> 注意：`tsconfig.json` 中 要设置 `"allowUnreachableCode": false` 编辑器才会有错误提示

<img src="https://jiahe-picbed.oss-cn-shenzhen.aliyuncs.com/typora-image/image-20250917120135423.png" alt="image-20250917120135423" style="zoom: 50%;" />	







## 字面量

### 字面量类型

![image-20250917130536327](https://jiahe-picbed.oss-cn-shenzhen.aliyuncs.com/typora-image/image-20250917130536327.png)	

### 对象字面量

![image-20250917130756694](https://jiahe-picbed.oss-cn-shenzhen.aliyuncs.com/typora-image/image-20250917130756694.png)	

#### 可选符号 `?`

#### readonly



## 类型别名与接口

### 类型别名 type

### 接口 interface



## 装箱类型与拆箱类型

在 Javascript 中，原型链的顶端是 `Object` 和 `Function` ，即所有基本类型和对象类型最终都指向 Object。在 Typescript 中就表现为 Object 包含了所有类型。

```typescript
const temp1: Object = { name: 'jack' };
const temp2: Object = () => {};
const temp3: Object = [];
const temp4: Object = new String("hello");
const temp5: Object = "world";
const temp6: Object = 123;
const temp7: Object = true;
const temp8: Object = Symbol("a");

// 关闭strictNullChecks，下面也成立
const temp9: Object = undefined;
const temp10: Object = null;
const temp11: Object = void 0;
```

这就是 **装箱类型**，类似的还有 String, Number, Boolean, Symbol，而 **拆箱类型** 则是 string, number, boolean 等等。

下图表示几种对象表示不同的值是否有效：

| 值                    | object | {}   | Object |
| --------------------- | ------ | ---- | ------ |
| `{}`                  | 是     | 是   | 是     |
| `[]`                  | 是     | 是   | 是     |
| `function(){}`        | 是     | 是   | 是     |
| `new String('hello')` | 是     | 是   | 是     |
| `'a'`                 | 否     | 是   | 是     |
| `123`                 | 否     | 是   | 是     |
| `Symbol('a')`         | 否     | 是   | 是     |
| `null`                | 否     | 否   | 否     |
| `undefined`           | 否     | 否   | 否     |



## 联合类型 “ | ”

```typescript
type Student = { name: string, score: number };
type Teacher = { name: string, age: number, subject: string };
type Person = Student | Teacher;

const person1: Person = { name: "jack", score: 100 };
const person2: Person = { name: "jack", age: 18, subject: "math" };
const person3: Person = { name: "jack", age: 18, subject: "math", score: 100 };
const person4: Person = { name: "jack" }; // error
```

从以上代码可以看出，联合类型 `Person` 的值可以是 `Student` 的值，也可以是 `Teacher` 的值，甚至可以是两者 **合并后的值**。

如果是两者合并后的值，那么在类型提示时，只会提示 **共同的属性**



## 交叉类型 " & "

不可以把拆箱类型（字面量类型或基础类型）用 `&` 进行交叉，结果会是 `never`

在实际开发中，可以使用交叉类型对联合类型进行约束

```typescript
type Params = string | number | boolean
type Pt = Params & string; 	// string
```

也可以实现类似 **继承** 的效果

```typescript
type Good = {
  id: number
  name: string
  price: number
}

type Cart = Goods & {
  count: number
}

type Order = Goods & {
  count: number
  totalPrice: number
}

const goods: Goods = {
  id: 1,
  name: 'goods',
  price: 100
}

const cart: Cart = {
  id: 1,
  name: 'goods',
  price: 100,
  count: 1
}

const order: Order = {
  id: 1,
  name: 'goods',
  price: 100,
  count: 1,
  totalPrice: 100
}
```





## typeof 与 “控制流分析”

### typeof 类型查询

除了 Javascript 中检查变量的类型外，在 Typescript 中，typeof 被赋予了一个新的功能：类型查询

typeof 会返回 **类型推导** 自动判断的类型，对象也是可以的

```typescript
let temp1 = "hello1";
const temp2 = "hello2";
const temp3 = null;
const temp4 = (a: string) => a.toUpperCase();

type Temp1 = typeof temp1; //string
type Temp2 = typeof temp2; //hello2
type Temp3 = typeof temp3; //null
type Temp4 = typeof temp4; // (a: string) => string

const user = {
  name: 'jack',
  age: 18,
  address: {
    province: '四川',
    city: '成都'
  }
}

type User = typeof user;

const person: User = {
  name: 'jack',
  age: 18,
  address: {
    province: '四川',
    city: '成都'
  }
}
```



### 类型控制流分析

Typescript 可以随着代码逻辑，不断地尝试类型的收窄，这一行为叫做 “类型控制流分析”

```typescript
function parse(value: number | string | boolean | null | undefined) {
  if (typeof value === "number") {
    return value * 2;       // number
  } else if (typeof value === "string") {
    return `hello ${value}`; // string
  } else if (typeof value === "boolean") {
    return !value;            // boolean
  } else {
    return value;             // null | undefined
  }
}
```

> 在类型的控制流分析下，每进入一个 if 判断，联合类型的分支就会少一个，因为当前分支已经处理过对应的类型，不会进入下一个 if 分支中了。



## instanceof 和 in



## 字面量类型检查（可辨识联合类型）

在创建联合类型时，最好是创建 **可辨识联合类型**，而不是可选字段

```typescript
type Dog = {type: "Dog", name: string, age: number}
type Cat = {type: "Cat", name: string, age: number}
type Animal = Dog | Cat	// 可辨识联合类型，即可以在控制流分析中，通过 type 字段，来判断动物的类型

// 可选字段
// type Animal = {type?: "Dog" | "Cat", name: string, age: number}

function getAnimal(animal: Animal) {
    if(animal.type === "Dog") {
        return animal.name  // Dog
    } else if(animal.type === "Cat") {
        return animal.name  // Cat
    } else {
        const _neverCheck: never = animal
        throw new Error(`Invalid animal type: ${_neverCheck}`)
    }
}
```



## 自定义守卫 is

自定义守卫是以 ` {形参 a} is {类型 A}` 的形式，给 **返回值类型为布尔值** 的函数使用，如果返回 `true` 则代表 a 的类型是 A

**类型收窄只能在同一函数中** ，在不同函数中就不起作用

```typescript
function isString(input: any): input is string {
    return typeof input === "string"
}

function getInputType(input: string | number) {
    if(isString(input)) {
        console.log(input)
    } else {
        console.log(input)
    }
}
```



vue 的 `isRef` 和 `unRef` 就用到了自定义守卫的功能

```typescript
export function isRef (r: any): r is Ref {
    return !!(r && r.__v_isRef === true)
}

export function unRef<T>(ref: MaybeRef<T> | ComputedRef<T>): T {
    return isRef(r) ? r.value : r;
}
```



上述的 **字面量类型检查**、**instanceof**、**in**、**typeof**，在 Typescript 中统一称作为 **类型守卫**，目的是在做控制流分析时帮助 Typescript 做 **类型收窄**，便于类型推导



## 数组与元组

### 数组

### 元组

元组类型是数组类型的子类型

在 `TypeScript4.0` 中，加入了 具名元祖

```typescript
const pointer3: [x:number, y:number] = [20, 30];
const user:[name:string, age:number, gender:"男"|"女"] = ["jack",20,"男"]
```

为了避免通过 `.push` 方法修改元组，可以使用 `readonly` 修饰符

```typescript
const pointer3: readonly [x:number, y:number] = [20, 30];
pointer3.push(40);
```

![image-20250917154430762](https://jiahe-picbed.oss-cn-shenzhen.aliyuncs.com/typora-image/image-20250917154430762.png)	

## 方括号运算符

数组当然需要使用 `[]`，在 Javascript 中我们经常使用 `[]` 来获取数组的值，或者动态引用获取对象属性的值

```typescript
const arr = ["a", "b", "c", "d", "e"];
console.log(arr[1]); // b

const a = "name";
const obj = {
  id: 1,
  name:'jack'
}
console.log(obj[a]);  // jack
```

在 Typescript 中，方括号运算符 `[]` 用于类型计算，取出对象类型的键对应的值的类型，比如 `类型[键名]`，简写为 `T[K]` 会返回 `T` 类型的属性 `K` 的类型。

```typescript
type Person = {
  age: number;
  name: string;
  sex: boolean;
};

type Age = Person['age']; // number类型
```

方括号的参数如果是联合类型，那么返回的也是联合类型。

```typescript
type AgeOrName = Person['age'|'name']; // string | number
```

甚至可以获取数组的具体类型，注意下面的写法：

```typescript
const arr = ["a", "b", "c", "d", "e"];
type ArrType = typeof arr[number]; // string
```

因为在 Javascript 中，数组其实就是 `key:value` 的键值对，而数组的键也就是下标都是 number 类型

同样，如果是一个对象字面量类型的数组，一样会得到数组中对象字面量类型：

```typescript
type User = {
  name: string;
  age: number;
};

const users: User[] = [
  { name: "John", age: 25 },
  { name: "Steve", age: 30 },
  { name: "Mike", age: 35 },
];

type ArrType2 = typeof users[number]; // { name: string; age: number;}
```

如果是一个元组，就可以得到元组类型中所有位置上类型的联合类型：

```typescript
const roles: ["Admin", "User", "Guest"] = ["Admin", "User", "Guest"];
type ArrType3 = typeof roles[number]; // "Admin" | "User" | "Guest"
```





## 类型断言 as

![类型断言_1758095338500](https://jiahe-picbed.oss-cn-shenzhen.aliyuncs.com/typora-image/类型断言_1758095338500.svg)	

### 联合类型断言

```typescript
type Student = { name: string, score: number };
type Teacher = { name: string, age: number, subject: string };
type Person = Student | Teacher;

function print(person: Person) {
  console.log(person.name);
  console.log((person as Student).score)
}

print({ name: "John", score: 100});
```



### 父类型断言为子类型

```typescript
const inputDom = document.querySelector("input");
inputDom!.addEventListener("change", e => { 
  console.log((e.target as HTMLInputElement).value);
})
```



### 任意类型断言为 any

```typescript
(window as any).foo = 1;
```



### any/unknown 断言为任意具体类型

一些第三方库未定义好类型，或历史遗留问题，或复杂场景不好定义具体类型时，我们可以将其断言为具体的类型。



### 非空断言 !

### 双重断言

### as const 断言

指示 Typescript **将变量断言为常量**，例如：

1. 数组 断言为 只读元组
2. 对象的属性断言为 **只读属性**
3. 复杂的嵌套对象，断言为 **对象字面量** 类型

结合 **方括号运算符** 将数组的内容转换为为联合类型

```typescript
const roles = ["角色列表", "用户删除", "用户查询", "权限详情"] as const	
// const roles: readonly ["角色列表", "用户删除", "用户查询", "权限详情"]

type Role = typeof roles[number]	// type Role = "角色列表" | "用户删除" | "用户查询" | "权限详情"
```



## statisfies



## 枚举

